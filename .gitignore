# dependencies
node_modules/

# opencode runtime/cache
.opencode/auth.json

# opencore local data (long-term memory, budget) lives in ~/.opencore, but ignore any
# stray local copies just in case
.opencore/

# internal dev notes (not for public repo)
PROGRESS.md
HERMES_ANALYSIS.md

# env
.env
.env.*
!.env.example
!.env.docker.example

# OS noise
Thumbs.db
.DS_Store
